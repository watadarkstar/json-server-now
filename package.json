{
  "name": "json-server-now",
  "version": "0.0.1",
  "dependencies": {
    "faker": "^4.1.0",
    "json-server": "^0.14.2"
  },
  "scripts": {
    "start": "node index.js",
    "now:deploy": "now"
  }
}
